# --- STRICT MODE (Recommended) ---
# This ensures type safety and catches potential issues early
# If you encounter errors, fix them rather than enabling this
# strict-peer-dependencies=true

# --- COMPATIBILITY MODE (Only if needed) ---
# Use this only if you're forced to work with incompatible libraries
# It disables peer dependency checks
legacy-peer-deps=true
